[project]
name = "movie-management-api"
version = "1.0.0"
description = "FastAPI backend for Movie Management System"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "supabase>=2.0.0",
]
